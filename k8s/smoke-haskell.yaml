apiVersion: v1
kind: Namespace
metadata:
  name: dev
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-smoke-haskell
  namespace: dev
data:
  default.conf: |
    server {
      listen 80;
      server_name _;

      location /api/ {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://backend:8080;
      }

      location /visit/ {
        rewrite ^/visit/?(.*)$ /$1 break;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://visit-frontend:80;
      }

      location / {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://lms-frontend:80;
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-haskell
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-haskell
  template:
    metadata:
      labels:
        app: backend-haskell
    spec:
      containers:
        - name: backend
          image: ghcr.io/alberto-kali/lms-haskell-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: dev
spec:
  selector:
    app: backend-haskell
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lms-frontend-haskell
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lms-frontend-haskell
  template:
    metadata:
      labels:
        app: lms-frontend-haskell
    spec:
      containers:
        - name: lms-frontend
          image: ghcr.io/alberto-kali/lms-lms-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: lms-frontend
  namespace: dev
spec:
  selector:
    app: lms-frontend-haskell
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: visit-frontend-haskell
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visit-frontend-haskell
  template:
    metadata:
      labels:
        app: visit-frontend-haskell
    spec:
      containers:
        - name: visit-frontend
          image: ghcr.io/alberto-kali/lms-visit-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: visit-frontend
  namespace: dev
spec:
  selector:
    app: visit-frontend-haskell
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-smoke-haskell
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-smoke-haskell
  template:
    metadata:
      labels:
        app: nginx-smoke-haskell
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
      volumes:
        - name: nginx-config
          configMap:
            name: nginx-smoke-haskell
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-smoke-haskell
  namespace: dev
spec:
  type: LoadBalancer
  selector:
    app: nginx-smoke-haskell
  ports:
    - port: 80
      targetPort: 80
